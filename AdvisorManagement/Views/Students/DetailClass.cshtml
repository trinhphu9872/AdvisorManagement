
@{
    ViewBag.Title = "DetailClass";
    Layout = "~/Views/Shared/_Layoutvlu.cshtml";
    int i = 1;
    List<AdvisorManagement.Models.ViewModel.ListStudent> listStudent = (List<AdvisorManagement.Models.ViewModel.ListStudent>)ViewBag.detailClass;
}

<div class="container-fluid">
    <!-- ============================================================== -->
    <!-- Start Page Content -->
    <!-- ============================================================== -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                @if (ViewBag.classInfo != null)
                {
                    List<AdvisorManagement.Models.ViewModel.AdvisorClass> classInfo = (List<AdvisorManagement.Models.ViewModel.AdvisorClass>)ViewBag.classInfo;
                    foreach (var item in classInfo)
                    {
                        <div class="card-body">
                            <h5 class="card-title mb-0">VLU - @item.semester</h5>
                        </div>
                        <form action="" method="post">
                            <div class="input-box">
                                <label for="ngaysinh" style="margin-left: 10px;">Tên lớp:<input style="margin-left: 2px; width: 250px;" disabled tyle="text" value="@item.idClass"></label>
                                <label for="ngaysinh" style="margin-left: 10px;">Giáo viên:<input style="margin-left: 28px; width: 250px;" disabled tyle="text" value="@item.name"></label><br>
                                <label for="ngaysinh" style="margin-left: 10px;">Email:<input style="margin-left: 15px; width: 250px;" disabled tyle="text" value="@item.email"></label>
                                <label for="ngaysinh" style="margin-left: 10px;">Số điện thoại:<input style="margin-left: 4px; width: 250px;" disabled tyle="text" value="@item.phone"></label><br>
                            </div>
                        </form>
                        if (ViewBag.role == "Advisor" || ViewBag.role == "Admin")
                        {
                        <div class="page-breadcrumb">
                            <div class="row">
                                <div class="col-12 d-flex no-block align-items-center">
                                    <a href="" class="btn btn-success" style="margin-bottom: 5px; margin-left: 5px; width: 150px;">
                                        Thêm<i class="fas fa-plus"></i>
                                    </a>
                                   <a onclick="ImportExcelFile()" id="btnUpdate" class="btn btn-success" style="margin-bottom: 5px; margin-left: 5px; width: 150px;">
                                        Import Excel<i class="fas fa-plus"></i>
                                    </a>
                                    <a href="@Url.Action("ExcelExport", "Students")" class="btn btn-success" style="margin-bottom: 5px; margin-left: 5px; width: 150px;">
                                        Export Excel<i class="fas fa-plus"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                        }
                    }
                }
                </div>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title"></h5>

                        @*<div class="searchbar">
                            <input id="myInput" onkeyup="myFunction()" class="search_input" type="text" name="" placeholder="Search...">
                            <a href="#" class="search_icon">
                                <i class="fas fa-search"></i>
                            </a>
                        </div>*@
                        <div class="table-responsive">
                            @if (ViewBag.detailClass != null)
                            {
                               
                                <table id="zero_config" class="table table-striped table-bordered">
                                    <thead>
                                        <tr>
                                            <th width="10%" style="text-align:center">STT</th>
                                            <th width="10%" style="text-align:center">MSSV</th>
                                            <th width="25%" style="text-align:center">Họ và tên</th>
                                            <th width="20%" style="text-align:center">Email</th>
                                            <th width="10%" style="text-align:center">SDT</th>
                                            <th width="10%" style="text-align:center">Khóa</th>
                                            <th width="15%" style="text-align:center">Trạng thái</th>
                                            @if (ViewBag.role == "Advisor" || ViewBag.role == "Admin")
                                            {
                                                <th width="20%" style="text-align:center">Hoạt động</th>
                                            }
                                        </tr>
                                    </thead>
                                    <tbody id="myTable">
                                        @foreach (var item in listStudent)
                                        {
                                            <tr id="@item.id">
                                                <td style="text-align: center ;">@i</td>
                                                <td style="text-align: center ;">@item.idStudent</td>
                                                <td style="text-align:center">@item.name</td>
                                                <td style="text-align:center">@item.email</td>
                                                <td style="text-align:center">@item.phone</td>
                                                <td style="text-align:center">@item.course</td>
                                                <td style="text-align:center">@item.status</td>
                                                @if (ViewBag.role == "Advisor" || ViewBag.role == "Admin")
                                                {
                                                    <td style="text-align:center">
                                                        <a href="@Url.Action("UpdateStudent", "Students", new {id = item.idAcc })" id="btnUpdate" class="btn btn-primary">
                                                            <i class="fas fa-edit"></i>
                                                        </a>
                                                        <button name="delete" class="btn btn-danger">
                                                            <i class="fas fa-trash-alt"></i>
                                                        </button>
                                                    </td>
                                                }
                                            </tr>
                                            i++;
                                        }
                                    </tbody>
                                </table>
                            }
                            else
                            {
                                <p>Bạn đang không thực hiện cố vấn lớp nào</p>
                            }
                        </div>
                        @*@if (ViewBag.role == "Admin")
                        {
                            @Html.ActionLink("Back to List", "Index", "ClassManagements" , new {area = "Admin"}, null)
                        }
                        else
                        {
                            @Html.ActionLink("Back to List", "Index")
                        }*@
                    </div>
                </div>
            </div>
    </div>
</div>
<div id="myModal" class="modal fade" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Import danh sách sinh viên</h4>
            </div>
            @if (ViewBag.classInfo != null)
            {
                List<AdvisorManagement.Models.ViewModel.AdvisorClass> idClass = (List<AdvisorManagement.Models.ViewModel.AdvisorClass>)ViewBag.classInfo;
                foreach (var item in idClass)
                {
                    <div class="">
                        <div class="col-md-12" style="padding-bottom: 12px;padding-top: 12px;">
                            <div class="col-md-4"><span>Chọn file:</span></div>
                            <div class="col-md-8">
                                @Html.Raw(ViewBag.Error)
                                <input name="postedFile" type="file" id="fileExcel" class="form-control" />
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button onclick="HideModal()" type="button" class="btn btn-default" data-dismiss="modal">Đóng</button>
                        <button id="btnUpload" class="btn btn-primary" type="submit">Import</button>
                    </div>
                }
            }
        </div>
    </div>
</div>
@*<div class="container h-100">
        <div class="d-flex justify-content-center h-100">
            <a href=@Url.Action("create", "ClassManagements",new {area = "Admin"}) class="btn btn-primary" style="margin-bottom: 2%;margin-left:5%; width: 170px;">
                THÊM LỚP HỌC  <i class="fas fa-plus"></i>
            </a>
            <div class="searchbar">
                <input id="myInput" onkeyup="myFunction()" class="search_input" type="text" name="" placeholder="Search...">
                <a href="#" class="search_icon">
                    <i class="fas fa-search"></i>
                </a>

            </div>
            <div>
                <label for="cars">Chọn học kỳ:</label>

                <select id="semester" name="semester" onchange="myFunction()">
                    <option selected value="0">All</option>
                    @foreach (var item in hocky)
                    {
                        <option value="@item.semester_name - @item.scholastic">@item.semester_name - @item.scholastic</option>
                    }


                </select>
            </div>
        </div>
    </div>
    <div>


        <div>
            <table width="100%" class="table table-bordered table-striped table-responsive">
                <div class="card-body p-3">
                </div>

                <thead>
                    <tr>
                        <th width="10%">Stt</th>
                        <th width="25%" style="text-align:center">Mã lớp</th>
                        <th width="15%" style="text-align:center">CVHT</th>
                        <th width="15%" style="text-align:center">Học kỳ</th>
                        <th width="15%" style="text-align:center">Tổng SV</th>
                        <th width="20%" style="text-align:center">Hoạt động</th>


                    </tr>
                </thead>
                <tbody id="myTable">
                    @foreach (var item in Model)
                    {
                        <tr style="font-weight: bold">
                            <td style="text-align: center ;">@item.id</td>
                            <td style="text-align:center">@item.class_code</td>
                            @if (item.advisor_code != null)
                            {
                                foreach (var itemUser in nameUser)
                                {

                                    if (item.advisor_code.Equals(itemUser.user_code))
                                    {
                                        <td style="text-align:center">@itemUser.user_name</td>
                                    }




                                }
                            }
                            else
                            {
                                <td style="text-align:center"></td>
                            }

                            <td style="text-align:center">@item.semester_name</td>
                            <td style="text-align:center"></td>
                            <td style="text-align:center">
                                <a href="@Url.Action("EditClass", "ClassManagements", new { id = item.id })" id="btnUpdate" class="btn btn-primary">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a href="" id="btnDelete" class="btn btn-danger">
                                    <i class="fas fa-trash-alt"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            <ul class="pagination" style="margin-left: 70% ;">
                <li class="page-item"> <a class="page-link" href="#">&laquo;</a> </li>
                <li class="page-item active"><a class="page-link" href="#">1</a></li>
                <li class="page-item"><a class="page-link" href="#">2</a></li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item "><a class="page-link" href="#">...</span></a></li>
                <li class="page-item"><a class="page-link" href="#">50</a></li>
                <li class="page-item disabled">
                    <a class="page-link" href="#">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            </ul>
        </div>
    </div>*@
<script src="~/Assets/extra-libs/DataTables/datatables.min.js"></script>
<script>

    $("#zero_config").DataTable();
    function myFunction() {
        var input, filter, table, tr, td, i;
        input = document.getElementById("semester");
        filter = input.value.toUpperCase();
        table = document.getElementById("myTable");
        tr = table.getElementsByTagName("tr");
        for (i = 0; i < tr.length; i++) {
            td = tr[i].getElementsByTagName("td")[3];
            if (td) {
                if (td.innerHTML.toUpperCase().indexOf(filter) > -1) {
                    tr[i].style.display = "";
                } else {
                    tr[i].style.display = "none";
                }
            }
        }
    }

    $(document).ready(function () {
        $("#myInput").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#myTable tr").filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });

    function ImportExcelFile() {
        $("#myModal").modal("show");
    }

    function HideModal() {
        $("#myModal").modal("hide");
    }

    $(document).ready(function () {
        $('#btnUpload').click(function () {
            var formData = new FormData();
            var fileExcel = document.getElementById("fileExcel");
            var myFile = fileExcel.files[0];
            formData.append("postedfile", myFile);

            $.ajax({
                type: 'POST',
                url: '@Url.Action("ImportStudent", "Students")',
                contentType: false,
                processData: false,
                data: formData,
                success: function (data) {
                    if (data.success == false) {
                        alert(data.message);
                    } else {
                        $("#myModal").modal("hide");
                        alert(data.message);
                        location.reload();
                    }
                }
             })
        })
    })

    //Sự kiện xóa
    $(document).on('click', "button[name=delete]", function () {
        let id_std = $(this).closest('tr').attr('id');
        if (confirm("Bạn thực sự muốn xóa sinh viên này?")) {
            $.ajax({
                url: '@Url.Action("Delete", "Students")',
                type: 'POST',
                data: {
                    id: id_std
                },
                success: function (data) {
                     if (data.success == true) {
                         alert(data.message);
                         location.reload();
                     } else {
                         alert(data.message);
                     }
                }
            })
        }
    })


</script>

