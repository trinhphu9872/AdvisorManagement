
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layoutvlu.cshtml";
}


<div class="container-fluid">
    <!-- ============================================================== -->
    <!-- Start Page Content -->
    <!-- ============================================================== -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title mb-0">Danh sách lớp cố vấn</h5>
                </div>
            </div>

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"></h5>
                   
                    <div class="searchbar">
                        <input id="myInput" onkeyup="myFunction()" class="search_input" type="text" name="" placeholder="Search...">
                        <a href="#" class="search_icon">
                            <i class="fas fa-search"></i>
                        </a>
                    </div>
                    <div class="table-responsive">
                        @if (ViewBag.classAdvisor != null)
                        {
                            <table id="zero_config" class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th width="10%" style="text-align:center">STT</th>
                                        <th width="25%" style="text-align:center">Mã lớp học</th>
                                        <th width="15%" style="text-align:center">Người cố vấn</th>
                                        <th width="15%" style="text-align:center">Khóa</th>
                                        <th width="15%" style="text-align:center">Tổng SV</th>
                                    </tr>
                                </thead>
                                @if (ViewBag.classAdvisor != null)
                                {
                                    int i = 1;
                                    List<AdvisorManagement.Models.ViewModel.AdvisorClass> listClass = (List<AdvisorManagement.Models.ViewModel.AdvisorClass>)ViewBag.classAdvisor;
                                    <tbody id="myTable">
                                        @foreach (var item in listClass)
                                        {
                                            <tr>
                                                <td style="text-align: center ;">@i</td>
                                                <td style="text-align: center ;">
                                                    @Html.ActionLink(item.idClass, "DetailClass", new { classCode = item.idClass }, new { @style = "color:#333;margin-left:3px" })
                                                </td>
                                                <td style="text-align: center ;">
                                                    @item.name
                                                </td>
                                                <td style="text-align:center">K25</td>
                                                <td style="text-align:center">@item.amount</td>
                                            </tr>
                                            i++;
                                        }
                                    </tbody>
                                }
                                </table>
                        }
                        else
                        {
                            <p>Bạn đang không thực hiện cố vấn lớp nào</p>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- ============================================================== -->
    <!-- End PAge Content -->
    <!-- ============================================================== -->
    <!-- ============================================================== -->
    <!-- Right sidebar -->
    <!-- ============================================================== -->
    <!-- .right-sidebar -->
    <!-- ============================================================== -->
    <!-- End Right sidebar -->
    <!-- ============================================================== -->
</div>
@*<div class="container h-100">
        <div class="d-flex justify-content-center h-100">
            <a href=@Url.Action("create", "ClassManagements",new {area = "Admin"}) class="btn btn-primary" style="margin-bottom: 2%;margin-left:5%; width: 170px;">
                THÊM LỚP HỌC  <i class="fas fa-plus"></i>
            </a>
            <div class="searchbar">
                <input id="myInput" onkeyup="myFunction()" class="search_input" type="text" name="" placeholder="Search...">
                <a href="#" class="search_icon">
                    <i class="fas fa-search"></i>
                </a>

            </div>
            <div>
                <label for="cars">Chọn học kỳ:</label>

                <select id="semester" name="semester" onchange="myFunction()">
                    <option selected value="0">All</option>
                    @foreach (var item in hocky)
                    {
                        <option value="@item.semester_name - @item.scholastic">@item.semester_name - @item.scholastic</option>
                    }


                </select>
            </div>
        </div>
    </div>
    <div>


        <div>
            <table width="100%" class="table table-bordered table-striped table-responsive">
                <div class="card-body p-3">
                </div>

                <thead>
                    <tr>
                        <th width="10%">Stt</th>
                        <th width="25%" style="text-align:center">Mã lớp</th>
                        <th width="15%" style="text-align:center">CVHT</th>
                        <th width="15%" style="text-align:center">Học kỳ</th>
                        <th width="15%" style="text-align:center">Tổng SV</th>
                        <th width="20%" style="text-align:center">Hoạt động</th>


                    </tr>
                </thead>
                <tbody id="myTable">
                    @foreach (var item in Model)
                    {
                        <tr style="font-weight: bold">
                            <td style="text-align: center ;">@item.id</td>
                            <td style="text-align:center">@item.class_code</td>
                            @if (item.advisor_code != null)
                            {
                                foreach (var itemUser in nameUser)
                                {

                                    if (item.advisor_code.Equals(itemUser.user_code))
                                    {
                                        <td style="text-align:center">@itemUser.user_name</td>
                                    }




                                }
                            }
                            else
                            {
                                <td style="text-align:center"></td>
                            }

                            <td style="text-align:center">@item.semester_name</td>
                            <td style="text-align:center"></td>
                            <td style="text-align:center">
                                <a href="@Url.Action("EditClass", "ClassManagements", new { id = item.id })" id="btnUpdate" class="btn btn-primary">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a href="" id="btnDelete" class="btn btn-danger">
                                    <i class="fas fa-trash-alt"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            <ul class="pagination" style="margin-left: 70% ;">
                <li class="page-item"> <a class="page-link" href="#">&laquo;</a> </li>
                <li class="page-item active"><a class="page-link" href="#">1</a></li>
                <li class="page-item"><a class="page-link" href="#">2</a></li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item "><a class="page-link" href="#">...</span></a></li>
                <li class="page-item"><a class="page-link" href="#">50</a></li>
                <li class="page-item disabled">
                    <a class="page-link" href="#">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            </ul>
        </div>
    </div>*@

<script>


    function myFunction() {
        var input, filter, table, tr, td, i;
        input = document.getElementById("semester");
        filter = input.value.toUpperCase();
        table = document.getElementById("myTable");
        tr = table.getElementsByTagName("tr");
        for (i = 0; i < tr.length; i++) {
            td = tr[i].getElementsByTagName("td")[3];
            if (td) {
                if (td.innerHTML.toUpperCase().indexOf(filter) > -1) {
                    tr[i].style.display = "";
                } else {
                    tr[i].style.display = "none";
                }
            }
        }
    }

    $(document).ready(function () {
        $("#myInput").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#myTable tr").filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });
</script>
